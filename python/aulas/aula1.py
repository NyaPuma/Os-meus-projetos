
# calculo do consumo de combustivel de um carro
volume = float(input('Tamanho do deposito: '))
distancia = float(input('Quanto km percorreu: '))
preco = float(input('Preço do combustivel: '))

gasto = (volume / distancia) * 100

consumo = (volume * preco) 

print()